# デザイン方向性

このドキュメントは、Google Stitchでモックアップを生成する際の入力として使う。カラーパレットとページ種別ごとのUI要件を、ページ構成を決めた`docs/information-architecture.md`と対応づけてまとめてある。

## カラーパレット

| 役割 | 値 | 用途 |
|---|---|---|
| 背景 | #474d9a | ページ全体の背景色 |
| プライマリ | #96a3d1 | リンク、ボタン、見出しなどの強調要素 |
| セカンダリ | #100e13 | カードやパネルなど、背景よりさらに沈める要素 |
| テキスト | #ffffff | 本文・見出しの文字色 |

## トーン&マナー

背景に濃い青紫を敷き、明るい青紫のプライマリカラーで要素を浮かび上がらせるダークトーンを基調にする。セカンダリカラーの黒に近い色はカードやフォームの背景に使い、コンテンツの階層を色の濃淡で表現する。テキストは白で統一し、暗い背景の上でも可読性を確保する。

## ページ遷移

Vision Transition APIによるページ遷移アニメーションは控えめにする。切り替わりが瞬時に感じられる程度の微細な遷移感だけを付け、アニメーション自体を目立たせない。

## ページ種別ごとのUI要件

| ページ | URL | UI要件 |
|---|---|---|
| トップ | `/` | ヒーローエリアに自己紹介を配置し、技術記事一覧と制作実績一覧への導線を目立たせる |
| 記事一覧 | `/blog` | カード形式で記事タイトル、公開日、タグを一覧表示する |
| 記事詳細 | `/blog/$slug` | 本文の可読性を優先し、見出し構造に沿った目次を持たせる |
| 実績一覧 | `/projects` | カード形式でサムネイル、タイトル、技術スタックのタグを一覧表示する |
| 実績詳細 | `/projects/$slug` | 制作物のスクリーンショットまたはデモへのリンクを主役にする |
| 管理画面 | `/admin`配下 | 公開ページと視覚的に区別できる配色にし、フォームと一覧表を中心にした実務的なレイアウトにする |

## Stitchへの入力方法

上記のカラーパレットとページ種別ごとのUI要件をそのままプロンプトに渡せば、各ページのモックアップを生成できる。
